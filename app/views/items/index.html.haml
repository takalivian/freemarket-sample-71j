.wrapper
  = render "header"
  = render "main.top"
  = render "main.bottom"
  = render "footer"
- if user_signed_in?()
  = link_to  new_item_path do
    .exhibition
      .exhibition__exhibition-text
        出品する
      .exhibition__exhibition-icon